# 2026-07-18 学习日志 (Day 3 复习)

## 主题

**Day 3 全面复习** — 不学新知识,验证 Day 3 真实掌握程度。

## 今天做了什么

5 个 Part 的复习 + 评估,共 60 分钟。

### Part 1 - 知识回顾 (15 min)

用自己的话回答 5 个概念:
1. Agent 完整执行流程
2. Tool Registry 是什么
3. execute_tool 为什么能动态调用
4. JSON 字符串为什么要 json.loads()
5. Tool Result 为什么 append 到 messages

**表现**:整体方向对,Q3/Q4 需要追问才答到点。

### Part 2 - 代码阅读 (20 min)

逐文件回答"为什么存在 / 删了会怎样 / 在流程哪一步":
- tools.py: TOOLS / get_tool_descriptions / read_file_tool try-except
- llm.py: 独立文件意义 / choices[0] 数据类型
- agent.py: MAX_ROUNDS / TOOL_PATTERN 匹配失败 / append 顺序

**表现**:T3 try-except 原理只到结论级(明白结论,未懂原理)。

### Part 3 - Debug 复盘 (20 min)

6 道典型报错诊断(JSONDecodeError / ModuleNotFoundError / KeyError / TypeError / 哑错误 env / UTF-8 BOM):

| Bug | 类型 | 表现 |
|---|---|---|
| JSONDecodeError | 真错误 | 方向对,根因修了一半(未指出"LLM 输出不稳定 + 修复方向是重试") |
| ModuleNotFoundError | 真错误 | 答不出(我直接给答案) |
| KeyError | 真错误 | 答不出(我直接点破:字典大小写敏感) |
| TypeError | 真错误 | 答对了 try/except 让 LLM 重试 |
| 哑错误 (env) | 哑错误 | 答不出 |
| UTF-8 BOM | 哑错误 | 答不出(自己 Day 3 踩过) |

### Part 4 - 数据流默写 (15 min)

11 步完整默写 User → str → 正则 → dict → 函数 → append → LLM Final Response。

**表现**:11 步顺序全对,但漏了"正则提取"独立一步 + 把 append 目的说成"防止 system_prompt 丢失"(实际是给 LLM 看完整对话历史)。修正后通过。

### Part 5 - 综合练习 (10 min)

6 题混合题型:3 道判断 / 1 道填空 / 1 道预测 / 1 道代码修改 / 1 道 Debug / 1 道应用题。

**表现**:判断题全对 / 填空题 1 空错(填了 response,正确是错误字符串) / 预测题漏 [OUT] / Debug 方向对一半 / 代码修改对 / 应用题漏写工具函数本身。

## 8 级能力评估

| 维度 | 等级 | 理由 |
|---|---|---|
| Agent Loop | L5 解释原理 | 11 步数据流能完整默写,边界条件清晰 |
| Tool Calling | L4 独立写出 | 理解 str→dict→函数调用全链 |
| execute_tool | L4-L5 之间 | 动态性懂,字段语义模糊 |
| JSON | L5 解释原理 | 能区分 str vs dict 本质差异 |
| Debug | L3-L4 | 能定位报错行,哑错误意识有 |
| Git 工作流 | 未评估 | 今天未涉及 |

## 今天真正掌握了什么

1. 数据流 11 步完整闭环
2. 字典注册模式的动态性(加工具不改 execute_tool)
3. append 顺序的语义(assistant 决策 + user 工具结果)
4. 错误处理方向(LLM 输出不稳定 → try/except 让 LLM 重试)
5. 哑错误意识(env 静默返回 None / BOM 导致 key 找不到)

## 还有哪些薄弱点

1. try/except 原理只到结论级(明天补 Python 异常基础)
2. openai SDK 内部细节完全黑盒(Choice / Message 类型)
3. Python 错误类型语义不熟(TypeError / KeyError / JSONDecodeError 见过但分类不熟)
4. lambda / 装饰器 / 闭包等高阶函数空白
5. Pydantic / try-except / async 均未接触

## 明日判断

可以进入 Pydantic / 结构化输出,但建议先补 try/except 基础(30 min)。

主线 Phase 4 还有 3 项没收尾:
1. 工具结果结构化(Pydantic / JSON schema)
2. 错误处理与重试(try/except)
3. 多工具混合调用

不建议直接上 LangGraph / MCP。
