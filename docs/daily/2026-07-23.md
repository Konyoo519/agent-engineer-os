# 2026-07-23 学习日志 (Day 7)

## Learning Rule Improvement

今天发现：

第一次学习一个新的知识点时，直接预测运行结果，会导致学习效率下降。

因此新增永久教学规则：

所有新知识必须遵循：

原理

↓

字面

↓

真实代码

↓

真实运行结果

↓

总结规律

↓

预测

↓

练习

目的：

培养推理能力，而不是随机猜测代码。

## Tutorial 09 Part 1

### 今天完成了什么

Tutorial 08 已完成。今天开始共同设计 Tutorial 09，并将其命名为 `09_structured_agent`。

Part 1 的主题是：**结构化 Agent action 与工具调用解析**。

Tutorial 08 使用普通文本协议：

```text
[TOOL:read_file:{"path":"tools.py"}]
```

Tutorial 09 Part 1 改用 JSON action 协议：

```json
{
  "kind": "tool_calls",
  "tool_calls": [
    {"name": "read_file", "arguments": {"path": "tools.py"}},
    {"name": "print", "arguments": {"text": "读取完成"}}
  ]
}
```

新增代码：

- `ToolCall`：表示一个待执行的工具调用
- `AgentAction`：表示 Agent 当前响应是工具调用还是最终回答
- `parse_action()`：把 JSON 文本解析成结构化对象
- `describe_action()`：观察结构化结果，不执行工具

### 真实运行结果

```text
ACTION KIND: tool_calls
TOOL CALL COUNT: 2
CALL 1: read_file({'path': 'tools.py'})
CALL 2: print({'text': '读取完成'})
```

### 总结规律

Part 1 建立了第一条新边界：

> 解析 Agent 想做什么，与执行工具，不是同一个职责。

一个响应可以被解析为多个独立的 `ToolCall`，后续调度器可以逐个决定如何执行。

### 当前能力评估

- Tutorial 08 Agent Loop / Tool Calling：L4-L5（已掌握）
- Tutorial 09 结构化 action：L2-L3（已讲解并跑通，尚未完成独立练习）

### Part 1 练习（待完成）

在不修改 `parse_action()` 的前提下，修改 `demo_part1.py` 的 `RAW_RESPONSE`，使它表示：

1. 只有一个 `print` 工具调用；
2. 工具参数中的文本改成你自己的内容；
3. 运行程序并解释 `TOOL CALL COUNT` 和 `CALL 1` 为什么变化。

暂不要求修改执行器，也不要把工具真正执行起来。

### 下一步

完成练习后，再进入 Tutorial 09 Part 2：参数模型与嵌套参数验证。
